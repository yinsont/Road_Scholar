import React from 'react'

function Timer({ inGame, onGameStart }) {

  /*
    when inGame becomes true, start the timer
    when submit is pressed on the GuessForm, change isGame to false
  */

  return (
    <div id='timer'>
        <h1>Timer goes here</h1>
    </div>
  )
}

export default Timer